# Core dashboard
streamlit>=1.35.0
plotly>=5.22.0
numpy>=1.26.0

# REST API bridge (outbound calls)
requests>=2.31.0

# Unity bridge HTTP server (inbound from Unity)
fastapi>=0.111.0
uvicorn>=0.29.0
